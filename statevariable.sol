pragma solidity ^0.8.0;

contract stateVariables {
    // State variable
    uint public myUint = 123; // This is a state variable that is permanently stored in the contract storage. They are declared inside a contract and outside a function.
}
